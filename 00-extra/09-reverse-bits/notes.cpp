// - the same 32-bit pattern can represent different numbers depending on whether we interpret it as signed or unsigned.

// The idea is: take bits from n from right → left, and build result from left → right.