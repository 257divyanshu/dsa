// QUESTION NO 2006

// came up with two approaches:
// - first one was TC O(n^2) and SC O(1) brute-force
// - second one was TC O(n) and SC O(n) optimal

// 📍 .count() on unordered_map
// - checks whether a key exists in the map.
// - Returns 1 if key exists
// - Returns 0 if key doesn't exist